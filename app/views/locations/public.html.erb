<div id="content">
	<h1><%=@survey.question%></h1>
	<div id="ajax-div">
		
	</div>

</div>
<script type="text/javascript">
$(function(){
	//$('#ajax-div').load();
	loadAnswers();
});

function loadAnswers(){
	$.ajax({
	  url: '/locations/<%=@location.id%>/answers',
	  success: function(data) {
	    $('#ajax-div').html(data);
	  },
	  beforeSend: function(data) {
	    $('#ajax-div').html('<img src="/images/ajax.gif" alt="" />');
	  }
	});
}

function vote(location_id, answer_id){
	$.ajax({
	  url: '/locations/' + location_id + '/vote',
	  data: 'answer_id=' + answer_id,
	  success: function(data) {
	    //loadStats(location_id);
		loadAnswers();
	  },
	  beforeSend: function(data) {
	    $('#ajax-div').html('<img src="/images/ajax.gif" alt="" />');
	  }
	});
	return false;
}

function loadStats(location_id){
	//$('#ajax-div').load('/locations/'+location_id+'/stats');
	console.log('starting...');
	$.ajax({
	  url: '/locations/' + location_id + '/stats',
	  dataType: 'html',
	  success: function(data) {
		console.log('success..');
		console.log('data');
	     eval(data);
		console.log("Data "+data);
	  }
	});
}
</script>